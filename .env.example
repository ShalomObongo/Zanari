# Zanari2 root environment example
# Rename this file to `.env` (or configure your secrets provider) before running the app or API.
# Values below are safe defaults; replace them with real credentials for your environments.

########################################
# Expo / React Native app configuration
########################################
# Supabase project URL (anon-facing)
EXPO_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
# Supabase anon public key for client-side auth/storage access
EXPO_PUBLIC_SUPABASE_ANON_KEY=public-anon-key
# Paystack public key (sandbox by default). Never expose secret keys in the app bundle.
EXPO_PUBLIC_PAYSTACK_PUBLIC_KEY=pk_test_your_public_key
# Base URL for the Zanari API the mobile app should call.
# When developing locally with an API server running on port 3000, Expo typically needs your LAN IP.
EXPO_PUBLIC_API_URL=http://localhost:3000

########################################
# Node.js API service configuration
########################################
# Supabase service role key (full access). Keep secret.
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key
# Supabase REST/Realtime URL (same project as above).
SUPABASE_URL=https://your-project.supabase.co
# Optional: JWT secret if you mint custom tokens for Supabase auth.
SUPABASE_JWT_SECRET=super-secret-jwt

# Paystack secret key used for server-side charges and transfers (sandbox by default).
PAYSTACK_SECRET_KEY=sk_test_your_secret_key
# Optional: override Paystack API base URL (defaults to https://api.paystack.co).
PAYSTACK_BASE_URL=https://api.paystack.co
# Webhook secret for verifying Paystack events (use the same as secret key unless Paystack provides a separate value).
PAYSTACK_WEBHOOK_SECRET=sk_test_your_secret_key

# HTTP port for the API server (adjust if your server entry point uses a different default).
PORT=3000
# Standard environment indicator (development | test | production).
NODE_ENV=development

########################################
# Testing and tooling helpers (optional)
########################################
# When set, enables verbose Jest output during local runs.
# VERBOSE_TESTS=1
# Force consistent timezone for tests.
# TZ=UTC
